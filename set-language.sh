#!/bin/bash

# (opt) check if language files are already there and use them
# 1 export language variables
# 2 rewrite config files
# 3? write them to dotlang/{lang-code}/{configs}

# TODO actually do all dat